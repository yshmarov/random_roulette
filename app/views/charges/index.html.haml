%h1 Listing charges

%table
  %thead
    %tr
      %th User
      %th Amount

  %tbody
    - @charges.each do |charge|
      %tr
        %td= charge.user
        %td= charge.amount

%br

= link_to 'New Charge', new_charge_path
